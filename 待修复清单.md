# 前端待修复清单

> 记录 `vue-pure-admin`（customer-b）与后端 `eladmin-mp` 对照后尚未完成或存在问题的功能。  
> 更新时间：2026-07-10

---

## 修复优先级说明

| 级别 | 含义                                      |
| ---- | ----------------------------------------- |
| P0   | 菜单可见且功能不可用 / 数据错误，优先处理 |
| P1   | 有页面但半完成，或隐藏菜单中的实质缺口    |
| P2   | 后端已就绪但无前端，或 Demo / 遗留代码    |

---

## P0 — 高优先级

### 1. 抄表数据页 API 不匹配 — ✅ 已修复

### 2. 采集器写操作未接 API — ✅ 已修复

### 3. 电表编辑未持久化 — ✅ 已修复

### 4. 报警用量设置 — 页面做错 — ✅ 已修复（改为用量规则列表页）

### 5. 报警事件批量删除未实现 — ✅ 已修复（前后端 batch-delete）

---

## P1 — 中优先级

### 6. 报警系统设置 — 纯前端展示 — ✅ 已修复

- 后端 `POST /api/alarm-system-setting-get|save`（Redis 持久化）
- 规则评估引擎读取系统开关
- 侧栏已恢复展示「系统报警设置」

### 7. 电表「基本业务」弹窗部分为假数据 — ✅ 已修复

- 告警 Tab：对接 `getAlarmEventQueryList`（按 deviceId / meterNo）
- 操作记录 Tab：对接 `getOperationLogsList`（按 meterNo 模糊搜索）
- 移除合成时间线数据

### 8. 水表 / 添加表 — Mock 页（路由已移除）

- **页面**：`src/views/monitor2/water-meter/`、`meter-add/`
- **问题**：全 Mock；静态路由已从 `monitor2.ts` 移除
- **后端**：水表有 `GET /api/water-meters`，前端 `water-meter.ts` 未对接
- **修复方向**：若菜单仍暴露旧路径，需同步数据库菜单或恢复路由并接 API

### 9. 菜单与路由漂移 — ✅ 已修复

- `mock/asyncRoutes.ts` 与静态路由对齐（采集器 + 电表）
- `remaining.ts` 增加废弃路径重定向
- `router/utils.ts` 动态菜单遗留路径自动映射

### 10. 短信 / 外部报警通知

- **相关**：首页「短信服务：已暂停」；`NotifyMethodEnum` 含 SMS / 钉钉 / 微信
- **问题**：前端无通知渠道配置与发送 UI；后端报警通知发送亦未完整实现
- **修复方向**：优先 Webhook（钉钉 / 企微），短信作为可选通道（见后端方案讨论）

### 11. 权限管理 — 接口授权 — ✅ 已修复

- **页面**：`src/views/permission/page/index.vue`
- **后端**：`POST /api/external-auth-settings-get|save`（Redis 持久化 + 运行时覆盖 `external.api`）
- **字段映射**：授权码 → `auth-token` / `ele-read.auth-code`；同步开关 → `enabled`；随机字符串 → `anonymous-sync.secret`；接口模式 dev/prod → 签名/回调校验开关
- **请求次数**：Redis 计数，采集器 askData / getSign 出站时累加

### 12. 账户设置 — 个人信息 / 安全日志 — ✅ 已修复

- **页面**：`src/views/account-settings/`
- **个人信息**：`GET /auth/info` + `PUT /api/users/center`（昵称、手机号）；头像 `POST /api/users/updateAvatar`
- **手机号**：原「密保手机」简化为个人信息内直接编辑
- **安全日志**：`GET /api/logs/user` 分页
- **未做**：在线改密、密保问题、邮箱验证修改（邮箱只读）

### 13. 站内信（铃铛）— 消息 / 通知 / 待办 — ✅ 已修复

- **组件**：`src/layout/components/lay-notice/`
- **通知**：报警事件轮询 `POST /api/alarm-event-query`（`alarmNoticePoll` + Pinia 持久化）
- **消息**：`GET /api/logs/user` 当前用户操作日志（排除登录类）
- **待办**：`POST /api/alarm-event-query`，`alarmStatus=pending` 未处理报警
- **角标**：未读报警通知 + 待办条数

---

## P2 — 低优先级 / 后续

### 14. 采集器时序数据 — 无管理页

- **API**：`src/api/collector-data.ts` → `/api/collector-data`
- **后端**：`CollectorDataController` 已有
- **修复方向**：按需新增采集数据查询 / 统计页面

### 15. 手机验证码登录 / 注册

- **页面**：`src/views/login/components/LoginPhone.vue` 等
- **问题**：模拟登录，未接后端
- **结论**：管理后台暂不建议主推短信登录（见架构讨论）

### 16. eladmin 能力未做前端

- **缺失模块**：字典、定时任务、在线用户等
- **后端**：`DictController`、`QuartzJobController`、`/auth/online` 等
- **修复方向**：按运维需要选择性补齐

### 17. 在线用户页孤立

- **页面**：`src/views/monitor/online/index.vue`
- **问题**：无路由；API 路径与 eladmin 不一致

### 18. 开发环境 Mock 回退

- **文件**：`src/api/business-stats.ts`、`meter-template/hook.tsx`
- **问题**：非生产环境 API 失败时 fallback 演示数据，易掩盖联调问题
- **修复方向**：保留开关但默认关闭，或仅 test 路由使用

### 19. 遗留占位页清理

- `src/views/alarm/usage-settings/`、`system-settings/` — 「已移动到…」占位，未挂路由
- `src/views/monitor2/logs/system/` — 全 Mock + 多个「开发中」Tab，未在静态路由注册

### 20. 未使用的 API 文件（死代码）

- `alarm-management.ts`、`business-stats-new.ts`、`electric-meter-new.ts`、`device.ts`、`report.ts`、`tag.ts`、`data-collection.ts` 等
- **修复方向**：接入或删除，避免误导

---

## 已实现（无需重复开发）

| 模块            | 路由                       | 说明                                |
| --------------- | -------------------------- | ----------------------------------- |
| 首页 / 数据大屏 | `/welcome`、`/data-screen` | 已接采集器、电表、报警、能耗        |
| 系统管理        | `/system/*`                | 用户 / 角色 / 菜单 / 部门           |
| 系统监控日志    | `/monitor/*-logs`          | 操作 / 登录 / 异常日志              |
| 用量统计        | `/business-stats/*`        | 时 / 日 / 月 / 年统计               |
| 报警规则        | `/alarm/rule-config`       | 增删改查已接后端                    |
| 报警事件        | `/alarm/event-query`       | 列表、处理、清空已接                |
| 采集器列表      | `/monitor2/collector`      | 查询可用                            |
| 电表列表 / 删除 | `/monitor2/electric-meter` | 查询、删除可用                      |
| 接口授权        | `/permission/page`         | 已接 Redis + external.api           |
| 账户设置        | `/account-settings`        | 个人信息 / 头像 / 手机号 / 安全日志 |
| 站内信          | 顶部铃铛                   | 通知 / 消息 / 待办接真实 API        |

---

## 建议修复顺序

1. 抄表数据页（P0-1）
2. 采集器 / 电表编辑持久化（P0-2、P0-3）
3. 报警用量设置：重做或下架（P0-4）
4. 报警批量删除（P0-5）
5. 菜单与 Mock 页清理（P1-8、P1-9）
6. 报警外部通知（P1-10）

---

## 备注

- 纯 admin 框架 Demo 路由（table、form、chatai 等）已 `showLink: false`，不在本清单范围。
- 后端本地机密配置见 `eladmin-mp/.../application-local.yml`（gitignore）。
